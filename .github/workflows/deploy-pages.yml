name: Deploy Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy to GitHub Pages
        run: |
          npm install gh-pages
          npx gh-pages -d dashboard
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
